<div class="container fluid">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <ul class="media-list">
        {{#each model as |post|}}
          {{post-tile post=post}}
        {{/each}}  
      </ul>
    </div>
  </div>
</div>
